'use strict';

//menu if hovered over, other nav items get darker
const navHover = function (e, opacity) {
    //if(e.target.classList.contains())
    console.log(e.target)
    console.log('Test')
}
document.getElementById('nav-bar').addEventListener('mouseover', function (e) {
    navHover(e)
})

console.log('this is a test')